"""The void CLI: a Textual terminal UI over the runtime, in-process.

Repo-only: a client of the runtime's event protocol, not part of the
wheel. `python -m cli` runs it on the host; `make cli-build` packs it into
one binary.
"""
