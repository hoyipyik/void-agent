"""Reference provider adapters. Each module imports its SDK at the top, so
import only the provider you installed (`void-agent[anthropic]` /
`void-agent[openai]`); the core framework depends on neither."""
